#!/usr/bin/python3

def add_tuple(tuple_a=(), tuble_b=()):
    result ()
    for i in range(2):
        a_value = tuple_a[i] if i < len(tuple_a) else 0
        b_value = tuple_b[i] if i < len(tuple_b) else 0
        result += (a_value + b_value,)
        return result
